package stellaris.rule;
import arc.struct.*;
public enum RuleEnum {
	SencondRule;
	
	
	/*public static IntMap<RuleEnum> map;
	static {
	    map = new IntMap<>(values().length);
	    for(RuleEnum e : values()){
	        map.put(e.ordinal(), e);
	    }
	}*/
}